"""
Orchestration package -- multi-agent communication layer.

This package is self-contained and does NOT modify any files in
api/core/ or any teammate-owned modules. It imports read-only
from core/schemas.py for type compatibility, and defines all
orchestration-specific types locally.

Owned by: feature/multi-agent-comms branch
"""
